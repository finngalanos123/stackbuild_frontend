import { GetChoicesFormGroupPipe } from './get-choices-form-group.pipe';

describe('GetChoicesFormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new GetChoicesFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
