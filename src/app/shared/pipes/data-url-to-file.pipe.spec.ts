import { DataUrlToFilePipe } from './data-url-to-file.pipe';

describe('DataUrlToFilePipe', () => {
  it('create an instance', () => {
    const pipe = new DataUrlToFilePipe();
    expect(pipe).toBeTruthy();
  });
});
